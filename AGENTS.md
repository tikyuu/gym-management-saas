# 作業ルール

## このプロジェクトでの役割分担

- ユーザーの主軸はAWS、CDK、ECS、CI/CD、SRE、ObservabilityおよびSecurityとする。フルスタック実装を細部まで自力で書くことは目的としない
- フロントエンド、バックエンドAPI、DBテーブルおよびマイグレーションは、合意した機能範囲内でエージェントが提案・実装・テストしてよい
- アプリケーションは動作だけでなく、本番運用を見据えた認証・権限、デプロイ、監視、障害対応およびセキュリティを考慮する
- アプリケーション変更の説明は文法の逐語解説より、全体構造、設計理由、データと認証の流れ、運用上の影響を優先する
- AWSアーキテクチャ、CDK、ECS/Fargate、ネットワーク、IAM、CI/CD、監視、可用性、SRE、コスト、デプロイ・ロールバックは、ユーザーが重点的に理解・レビューできるよう説明する

## 進め方

- インフラの学習・変更は理解しやすい単位で進める。アプリケーション実装は関連する機能をまとめて進め、PRはレビュー可能な目的ごとに分ける
- 編集前に、何をするか・なぜ必要か・どこを変更するかを説明する
- 合意済みの作業範囲内でのアプリケーション実装は都度の編集許可を求めない。大きな設計変更は事前に理由と影響を説明し、ユーザーと合意してから行う
- 編集後は、変更したファイルを示し、重要な設計判断と確認結果を説明する
- 理解が曖昧な場合は先へ進まず、具体例や処理の流れに分解して説明する
- 初心者向けに簡略化した設計ではなく、実務で採用できる設計を優先する
- 技術を採用するときは、採用理由・別案・トレードオフを説明する

## Git

- `main`へ直接コミットせず、変更ごとにブランチを作成する
- 一つのPRで扱う目的を小さく保つ
- ユーザーが`PR`とのみ入力した場合は、現在のブランチ、変更ファイル、差分および確認結果を確認する
- 確認した差分に合わせて、`git add`、`git commit`、`git push`およびPR作成まで実行する
- PRタイトルと本文は日本語で作成し、変更内容と設計意図が無理なく理解できる、短すぎず長すぎない分量にする
- PRのマージは行わず、ユーザーの確認と判断に委ねる

## 検証

- 小さな変更ごとには、ビルドやCDK Synthを実行しない
- 作業途中の検証は、型や構成に不確実性がある場合だけ実行する
- PR前のビルドとCDK SynthはCIと重複して実行しない。CIで確認できない場合や不具合調査に必要な場合だけローカルで実行する
- PR作成後にCIの結果を確認し、実行中の場合はその旨を伝える
- 検証結果は、成功・失敗と重要な内容だけを簡潔に報告する
