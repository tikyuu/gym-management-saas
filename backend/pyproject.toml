[project]
name = "backend"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "alembic>=1.20.0",
    "boto3>=1.43.100",
    "fastapi[standard]>=0.141.1",
    "psycopg[binary]>=3.3.6",
    "pyjwt[crypto]>=2.14.0",
    "sqlalchemy>=2.0.54",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]

[tool.pytest.ini_options]
pythonpath = ["."]
